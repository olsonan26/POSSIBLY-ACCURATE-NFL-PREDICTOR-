{
  "name": "nfl-numerology-predictor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "backtest": "bun scripts/backtest.ts",
    "forward-test": "bun scripts/forward-test-2026.ts",
    "football-challenger": "bun scripts/pro-football-challenger-2025.mjs",
    "selective-market-correction": "bun scripts/selective-market-correction-2025.mjs",
    "verify": "bun run typecheck && bun run build && bun run backtest && bun run forward-test"
  },
  "dependencies": {
    "lucide-react": "^1.46.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
